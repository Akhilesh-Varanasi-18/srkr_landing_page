export const metadata = {
  title: 'Registrations Analytics',
  description: 'Internal ToriiMinds × SRKR registrations analytics dashboard.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function DashboardLayout({ children }) {
  return children;
}
